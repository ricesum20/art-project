
export interface Artwork {
  title: string;
  artist: string;
  description: string;
  image: string; // base64 encoded image string
}

export interface ArtworkInfo {
  title: string;
  artist: string;
  description: string;
}